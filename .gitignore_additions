# ============================================================
# 以下內容加入到 hermes-agent-iot/.gitignore 的末尾
# (Pi2 / IoT 優化：移除不必要的大型鎖定檔)
# ============================================================

# UV / pip lock files (在 Pi2 上用 pip 直接裝，不需要 uv.lock)
uv.lock

# Node.js lock file (Pi2 通常不跑 Node，lock 可在需要時 npm install 產生)
package-lock.json
yarn.lock

# Nix 環境 (Pi2 不使用 Nix)
flake.lock

# Docker (Pi2 1GB RAM 無法跑 Docker)
Dockerfile
docker-compose.yml
docker-compose*.yml

# llama.cpp 編譯產物 (clone 後在本機 make)
llama.cpp/
*.o
*.a

# HuggingFace 模型快取 (太大，透過 setup script 下載)
models/
*.gguf
*.bin
*.safetensors
.cache/huggingface/

# RAG 改用 honcho-ai（雲端），不再需要本機 chromadb/向量庫
# 以下為保留項目，避免舊資料意外被 commit
.chromadb/
chroma.sqlite3
~/work/work_RAG/
sentence_transformers/
